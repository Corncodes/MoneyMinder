# import os
# from psycopg_pool import ConnectionPool
# from models.expenses import ExpenseOut

# pool = ConnectionPool(conninfo=os.environ["DATABASE_URL"])

# class ExpenseQueries:
#     def get_expenses(self, id: int) -> ExpenseOut:
